import createMuiTheme from "@material-ui/core/styles/createMuiTheme";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#3d7ebc",
    },
    background: {
      hover: "#0c77be",
      disabled: "#aeaeb7",
    },
  },
});
