import createMuiTheme from "@material-ui/core/styles/createMuiTheme";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#3d7ebc",
    },
    background: {
      active: "#005e9d",
      selected: "#00437f",
      hover: "#0c77be",
      disabled: "#aeaeb7",
      outlineHover: "#e5f0f8",
    },
  },
});
